static_sites:
  - type: static
    name: student-union-frontend
    buildCommand: npm install && npm run build
    publishDirectory: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://your-backend-url.onrender.com/api/v1
